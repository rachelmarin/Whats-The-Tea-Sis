  <div class="container-fluid">

 <nav class="navbar navbar-expand-lg navbar-light bg-light">
   <a class="navbar-brand" href="#">
      <%= image_tag("logos/logo_size.jpg") %>
    </a>
        <span class="navbar-brand mb-0 h1"><%= link_to("Home", root_path) %></span>
         <% if user_signed_in? %>
        <span class="navbar-brand mb-0 h1"><%= link_to("Tea Recipes", recipes_path) %></span>
        <span class="navbar-brand mb-0 h1"><%= link_to("Create Recipe", new_recipe_path) %></span>
        <span class="navbar-brand mb-0 h1"><%= link_to("Category", categories_path) %></span>
       <span class="navbar-brand mb-0 h1"><%= button_to "Logout", destroy_user_session_path, method: :delete %></span>
      <% else %>   
       <span class="navbar-brand mb-0 h1"><%= link_to "Log in with Google", '/auth/google_oauth2', method: :post %> </span>
        <span class="navbar-brand mb-0 h1"><%= link_to("Login", login_path) %></span>
        <span class="navbar-brand mb-0 h1"><%= link_to("Signup", signup_path) %></span>
      
    </nav>
  </div>
    <% end %>
   