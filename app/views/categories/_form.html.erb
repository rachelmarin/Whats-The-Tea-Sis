<h3>Create Category</h3>

<%= form_for @category do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.hidden_field :user_id, value: current_user.id%>
  </div> 
   
    <%= f.submit %>
  <% end %>
  <br>
